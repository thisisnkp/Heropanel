root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/hpd.exe ./cmd/hpd"
  bin = ".\\tmp\\hpd.exe"
  include_ext = ["go", "tpl", "tmpl", "html"]
  include_dir = ["cmd", "internal", "pkg"]
  exclude_dir = []
  delay = 500
