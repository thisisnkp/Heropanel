go 1.23

// HeroPanel is a monorepo. The core (hpd), broker, and shared pkg/ live in the
// root module. Satellite modules (modules/*) are added here as independent
// modules as they are implemented, so each builds and versions independently.
use .
